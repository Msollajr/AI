# =============================================================================
# UDSM Student Support AI — Production Requirements
#
# Used by the Docker image build (Dockerfile).
# Excludes dev/UI tools (streamlit, pytest) to keep the image lean.
#
# Dev requirements (for local development):
#   pip install -r requirements.txt
# =============================================================================

# Web framework + server
fastapi>=0.111.0
uvicorn>=0.30.0

# HTTP clients
requests>=2.31.0
httpx>=0.27.0

# Data validation
pydantic>=2.0.0
python-multipart>=0.0.9

# Vector DB + embeddings
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# Security & Auth
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0
python-dotenv>=1.0.0
